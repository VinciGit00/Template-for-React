# ⚛️ Template for react

This repo allows to create a react project with a navbar and a footer with just one click.

To run the app change the directory and copy the code below

```bash
npm start
```
